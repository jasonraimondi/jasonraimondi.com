{
  "name": "jasontheme",
  "license": "MIT",
  "version": "0.0.0-rc1",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "normalize.css": "^8.0.1",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0",
    "postcss-custom-media-generator": "^1.1.0",
    "postcss-import": "^15.1.0",
    "postcss-mixins": "^9.0.4",
    "postcss-preset-env": "^8.4.2",
    "start-server-and-test": "^2.0.0",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "dev": "hugo serve -D",
    "build": "hugo --gc --cleanDestinationDir",
    "start:ci": "$HOME/hugo serve -D",
    "create-resume": "npm --prefix snapshotter run create-resume",
    "gen:resume": "start-server-and-test start http://localhost:1313 create-resume",
    "gen:resume:ci": "start-server-and-test start:ci http://localhost:1313 create-resume"
  },
  "browserslist": [
    "last 1 version",
    "> 1%",
    "maintained node versions",
    "not dead"
  ]
}
